import { StyleSheet, Text, View } from "react-native";
import React from "react";

const HSV = () => {
  return (
    <View>
      <Text>HSV</Text>
    </View>
  );
};

export default HSV;

const styles = StyleSheet.create({});
